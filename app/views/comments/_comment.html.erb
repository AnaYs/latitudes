<h4><%= comment.name %></h4>

<p> <%= comment.body %></p>

<p> <%= time_ago_in_words(comment.created_at) %></p>

<%= link_to 'delete', [comment.post, comment], method: :delete, class: 'btn btn-primary', data: { confirm: 'Are you sure?' } %>
